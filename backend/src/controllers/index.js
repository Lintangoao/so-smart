const auth = require('./auth');
const lap = require('./laporan');
const user = require('./users');
const perm = require('./permohonan');
const info = require('./informasi');

module.exports = {auth, lap, user, perm, info};